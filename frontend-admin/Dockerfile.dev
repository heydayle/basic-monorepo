FROM node:22-alpine

RUN apk add --no-cache git && \
    npm install -g pnpm@latest

WORKDIR /workspace

EXPOSE 3001

# Environment variables are now loaded from .env files via docker-compose
# No hardcoded ENV variables needed

CMD ["sh", "-c", "echo 'Use docker-compose to run specific commands'"]
